{
  "name": "local-webui",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first AI workspace optimized for small models",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@types/node-fetch": "^2.6.13",
    "@types/pg": "^8.18.0",
    "gray-matter": "^4.0.3",
    "node-fetch": "^3.3.2",
    "pg": "^8.20.0"
  }
}